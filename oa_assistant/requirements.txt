PySide6>=6.5.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
# For logging, we use standard library
# For OCR and AI, we'll add in later phases
# For screen capture, we'll add mss in later phases